# 插件名称变更：RunVSAgent → pabCoder

## 变更概述
将 IntelliJ IDEA 插件的名称从 `RunVSAgent` 更改为 `pabCoder`。

## 变更原因
根据需求调整插件品牌名称，使用新的名称 "pabCoder"。

## 变更详情

### 1. 插件配置文件
**文件：** `jetbrains_plugin/src/main/resources/META-INF/plugin.xml`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 10 | `<id>RunVSAgent</id>` | `<id>pabCoder</id>` | 插件唯一标识符 |
| 14 | `<name>RunVSAgent</name>` | `<name>pabCoder</name>` | 插件显示名称 |
| 60-61 | `id="RunVSAgent"` | `id="pabCoder"` | 工具窗口 ID |
| 65 | `id="RunVSAgent"` | `id="pabCoder"` | 通知组 ID |
| 74 | `id="RunVSAgent.extensionSwitcher"` | `id="pabCoder.extensionSwitcher"` | 扩展切换器 Action ID |
| 86 | `id="RunVSAgent.DynamicExtensionActions"` | `id="pabCoder.DynamicExtensionActions"` | 动态扩展 Actions Group ID |
| 90 | `ref="RunVSAgent.extensionSwitcher"` | `ref="pabCoder.extensionSwitcher"` | 扩展切换器引用 |
| 91 | `id="RunVSAgent.extensionStatusChecker"` | `id="pabCoder.extensionStatusChecker"` | 扩展状态检查器 Action ID |
| 98 | `id="RunVSAgent.RightClickMenu"` | `id="pabCoder.RightClickMenu"` | 右键菜单 Group ID |
| 99 | `id="RunVSAgent.RightClick.Chat"` | `id="pabCoder.RightClick.Chat"` | 右键聊天菜单 Group ID |
| 105 | `id="RunVSAgent.RightClick.Extensions"` | `id="pabCoder.RightClick.Extensions"` | 扩展管理菜单 Group ID |
| 106 | `ref="RunVSAgent.extensionSwitcher"` | `ref="pabCoder.extensionSwitcher"` | 扩展切换器引用 |
| 111 | `id="RunVSAgent.RightClick.ExtensionActions"` | `id="pabCoder.RightClick.ExtensionActions"` | 扩展动作菜单 Group ID |
| 112 | `id="RunVSAgent.RightClick.DynamicExtensionActions"` | `id="pabCoder.RightClick.DynamicExtensionActions"` | 动态扩展动作 Group ID |
| 120 | `id="RunVSAgent.MainMenu"` | `id="pabCoder.MainMenu"` | 主菜单 Group ID |
| 122 | `ref="RunVSAgent.extensionSwitcher"` | `ref="pabCoder.extensionSwitcher"` | 扩展切换器引用 |
| 123 | `id="RunVSAgent.MainMenu.DynamicExtensionActions"` | `id="pabCoder.MainMenu.DynamicExtensionActions"` | 主菜单动态扩展动作 Group ID |

### 2. 常量定义
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/util/PluginConstants.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 8 | `const val PLUGIN_ID = "RunVSAgent"` | `const val PLUGIN_ID = "pabCoder"` | 插件 ID 常量 |

### 3. 通知工具
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/util/NotificationUtil.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 18 | `private const val NOTIFICATION_GROUP_ID = "RunVSAgent"` | `private const val NOTIFICATION_GROUP_ID = "pabCoder"` | 通知组 ID 常量 |

### 4. 配置文件工具
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/util/ConfigFileUtils.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 57 | `"RunVSAgent Configuration"` | `"pabCoder Configuration"` | 配置文件注释 |
| 166 | `"RunVSAgent Extension Configuration - Default Template"` | `"pabCoder Extension Configuration - Default Template"` | 默认配置文件注释 |

### 5. 插件主类
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/plugin/WecoderPlugin.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 72 | `"Initializing RunVSAgent plugin for project"` | `"Initializing pabCoder plugin for project"` | 日志信息 |
| 95 | `"runvsagent.auto.create.config"` | `"pabcoder.auto.create.config"` | 系统属性名称 |
| 112 | `"-Drunvsagent.auto.create.config=true"` | `"-Dpabcoder.auto.create.config=true"` | 系统属性提示 |
| 141 | `"Disposing RunVSAgent plugin for project"` | `"Disposing pabCoder plugin for project"` | 日志信息 |
| 147 | `"RunVSAgent plugin initialized successfully"` | `"pabCoder plugin initialized successfully"` | 日志信息 |
| 153 | `"Failed to initialize RunVSAgent plugin"` | `"Failed to initialize pabCoder plugin"` | 错误日志 |
| 216 | `name = "RunVSAgent-ConfigMonitor"` | `name = "pabCoder-ConfigMonitor"` | 监控线程名称 |

### 6. 工具窗口工厂
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/ui/RunVSAgentToolWindowFactory.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 156 | `<div class='title'>🚀 RunVSAgent</div>` | `<div class='title'>🚀 pabCoder</div>` | UI 标题 |
| 567 | `"RunVSAgent System Information"` | `"pabCoder System Information"` | 系统信息标题 |
| 749 | `name = "RunVSAgent-ConfigMonitor-UI"` | `name = "pabCoder-ConfigMonitor-UI"` | UI 监控线程名称 |
| 1271 | `"Disposing RunVSAgent plugin for project"` | `"Disposing pabCoder plugin for project"` | 日志信息 |
| 1380 | `"RunVSAgent Debug Information"` | `"pabCoder Debug Information"` | 调试信息标题 |

### 7. 动态上下文菜单管理器
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/extensions/ui/contextmenu/DynamicContextMenuManager.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 134 | `actionManager.getAction("RunVSAgent.DynamicExtensionContextMenu")` | `actionManager.getAction("pabCoder.DynamicExtensionContextMenu")` | 动态扩展上下文菜单 Action ID |

### 8. 动态按钮管理器
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/extensions/ui/buttons/DynamicButtonManager.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 127 | `actionManager.getAction("RunVSAgent.DynamicExtensionActions")` | `actionManager.getAction("pabCoder.DynamicExtensionActions")` | 动态扩展动作 Action ID |

### 9. 主线程消息服务
**文件：** `jetbrains_plugin/src/main/kotlin/com/sina/weibo/agent/actors/MainThreadMessageServiceShape.kt`

| 行号 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 75 | `?: "RunVSAgent"` | `?: "pabCoder"` | 默认对话框标题 |
| 104 | `getNotificationGroup("RunVSAgent")` | `getNotificationGroup("pabCoder")` | 通知组 ID |

## 影响范围

### 代码变更
- 修改了 9 个文件
- 涉及约 30 处代码变更

### 功能影响
- ✅ 插件标识符从 "RunVSAgent" 变更为 "pabCoder"
- ✅ 所有 Action 和 Group ID 统一更新
- ✅ 所有日志信息和显示文本更新
- ✅ 所有通知组 ID 更新
- ✅ 所有配置文件注释更新

### 用户可见变更
- 插件在 IDEA 中的显示名称变为 "pabCoder"
- 工具窗口显示的标题变为 "pabCoder"
- 所有通知的来源显示为 "pabCoder"
- 菜单项和按钮的文本变为 "pabCoder"

## 注意事项

### 构建和部署
1. **必须清理并重新构建插件**：由于插件 ID 发生变更，需要清理旧的构建产物
2. **需要重启 IDEA**：插件 ID 变更后需要完全重启 IDE 才能生效
3. **需要重新安装插件**：旧版本的插件需要先卸载，再安装新版本

### 配置文件
- 用户配置文件位置保持不变：`~/.run-vs-agent/`
- 配置文件内容不受影响，可以继续使用

### 兼容性
- ⚠️ **不兼容旧版本**：由于插件 ID 变更，旧版本的配置和状态无法迁移
- ⚠️ **需要重新配置**：用户可能需要重新配置插件设置

### 测试建议
1. 测试插件是否能正常加载和初始化
2. 测试工具窗口是否正常显示
3. 测试通知是否正常工作
4. 测试右键菜单和主菜单是否正常显示
5. 测试扩展切换功能是否正常
6. 测试日志是否正常记录

## 回滚方案
如果需要回滚到 "RunVSAgent"：

1. 使用 Git 恢复到修改前的版本
2. 重新构建插件
3. 卸载新版本插件
4. 安装旧版本插件
5. 重启 IDEA

## 变更日期
2025-12-28

## 变更人
AI Assistant
